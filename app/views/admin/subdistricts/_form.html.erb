<%= simple_form_for [:admin, @subdistrict], :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name %>
  <%= f.input :latitude %>
  <%= f.input :longitude %>

  <div class="form-actions">
    <%= f.submit "Simpan", :class => 'btn btn-primary' %>
    <%= link_to "Batal", admin_subdistricts_path, :class => 'btn' %>
  </div>
<% end %>