<div>
  <%= paginate comments %>
</div>

<% unless comments.empty? %>
	<%= render :partial => 'opinio/comments/comment', :collection => comments %>
<% else %>
	<div id="no_comments"><%= t('opinio.messages.no_comments_found') %></div>
<% end %>

<div>
  <%= paginate comments %>
</div>
