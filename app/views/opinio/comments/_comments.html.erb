<div>
  <%= paginate comments %>
</div>

<% unless comments.empty? %>
	<%= render :partial => 'opinio/comments/comment', :collection => comments %>
<% else %>
	<div id="no_comments">Belum ada komentar</div>
<% end %>

<div>
  <%= paginate comments %>
</div>
